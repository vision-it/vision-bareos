Storage {
  Name = DefaultStorage
  Address = <%= @storage_hostname %>
  Password = <%= @storage_password %> # password for Storage daemon
  Device = "LocalFile"                # same as Device in Storage daemon
  Media Type = File                   # same as MediaType in Storage daemon
}
